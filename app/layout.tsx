import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SKUL - Aprende Inglés con Inteligencia Artificial",
  description: "Plataforma de aprendizaje de inglés impulsada por IA que ofrece lecciones personalizadas, conversaciones reales con inteligencia artificial y seguimiento detallado de progreso. Transforma tu inglés de manera efectiva y adaptada a tu ritmo.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
